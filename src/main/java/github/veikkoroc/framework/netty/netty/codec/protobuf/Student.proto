syntax = "proto3"; // 版本
option java_outer_classname = "StudentPOJO"; // 生成的外部类名，同时也是文件名

// protobuf 使用message方式来管理数据
message Student{ // 会在 StudentPOJO 外部类生成一个内部类，它是真正发送POJO对象

  int32 id = 1; //  student 类中有一个属性，名字为 id，类型为int32（protobuf类型） 1表示属性序号，不是值
  string name = 2;

}